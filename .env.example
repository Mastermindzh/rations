# Generate with: openssl rand -base64 48
SESSION_SECRET=replace-with-a-random-secret-of-at-least-32-characters

# Optional host port for Docker Compose (the container still listens on 3000).
# PORT=3000
